rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() { return request.auth != null; }
    function admin() { return signedIn() && request.auth.token.email == 'YOUR_GMAIL@gmail.com'; }
    match /{allPaths=**} {
      allow read: if signedIn();
      allow write: if admin() && request.resource.size <= 200 * 1024 * 1024;
      allow delete: if admin();
    }
  }
}
